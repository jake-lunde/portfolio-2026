# Architecture Decision Records

Hard-to-reverse **technical** decisions and their why — the lane rules
are in `agents/domain.md`. One decision per file,
`NNNN-kebab-title.md`, numbered from `0001`. Keep each to: Context ·
Decision · Consequences, a page or less. Created lazily by
`/domain-modeling` — don't backfill history for its own sake.

This directory is committed on purpose (the repo's `docs/` stays
private — CLAUDE.md §3.3); nothing personal or metric-bearing
belongs here.
