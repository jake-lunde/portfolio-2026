# deps
/node_modules

# next
/.next/
/out/
next-env.d.ts

# storybook
storybook-static/

# misc
.DS_Store
*.pem
.vercel

# env
.env*.local

# ts
*.tsbuildinfo

# local-only source docs — internal metrics, coworker names, reference
# images. Keep out of the public repo; the case-study MDX is the public copy.
portfolio-tracker.md
portfolio-tracker/
session-log.md
invest-pull-quotes.md
docs/

# raw ride exports (contain precise home GPS) — baked JSON is the public copy
ref/

# token build intermediates (concatenated into tokens.generated.css)
/src/styles/generated/
build-storybook.log

# figma plugin build output (built locally, loaded via manifest)
/figma-plugin/dist/

# ntsc-bake sources — the baked box-film.mp4 ships, the original stays local
public/case/**/*.src.mp4

# rust build output for the ntsc-rs wasm bridge (the .wasm itself is committed)
native/ntsc/target/
