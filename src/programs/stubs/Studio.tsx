import { UnderConstruction } from '@/components/primitives/UnderConstruction'

export default function Studio() {
  return <UnderConstruction note="Jake's own recordings. The tape is still being spliced." />
}
